version: 1
on:
  push:
    deployments:
      - target: dev
        cwd: /usr/src/eliware/path
        commands:
          - git pull --ff-only
          - npm ci
          - npm test
          - npm run lint
          - npm run typecheck
          - npm audit --omit=dev --audit-level=moderate
          - npm run pack
